/**
 * Level: App -> Processes -> Pages -> Widgets -> Features -> Entities -> Shared
 * Name: Shared
 * Slice: Reusable resources
 * Structure:
 *      - api/      # API request logic
 *      - config/   # app shared configuration
 *      - lib/      # infrastructure logic (helpers/utils/hooks)
 *      - ui/       # uikit library
 */

export {};
